{
  "functions": {
    "source": ".",
    "runtime": "nodejs20",
    "predeploy": [
      "npm --prefix \"$RESOURCE_DIR\" run build:functions"
    ]
  },
  "firestore": {
    "indexes": "firestore.indexes.json"
  }
}
